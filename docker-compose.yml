version: '3.8'
services:
  reyohoho:
    build: .
    image: reyohoho:latest
    container_name: reyohoho-dev
    ports:
      - "5173:5173" 
    environment:
      - HOST=0.0.0.0
    working_dir: /app
    restart: unless-stopped